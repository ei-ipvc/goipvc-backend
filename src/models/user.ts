export interface User {
  username: string;
  customName: string;
  theme: string;
  colorScheme: string;
  schoolTheme: string;
  lessonNotifs: number;
  taskNotifs: number;
  lastSeen: string;
  requests: number;
}
